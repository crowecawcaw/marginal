[ ] bug: toggle presentation command only goes to code view. should toggle. fix and add unit test
[ ] bug: markdown code is highlighted but the caret does not light up with rendered text. looks like the editable text and rendered text have different placements or margins or sometinn. fix it
[ ] improve: too much markdown highlighting. make headings blue, link text pink, link href blue, image href blue, code organe. but regualr text is white.
[ ] make cmd hints pop up after .8s instead of 1
[x] add table support to presentation view. should render tables, allow editing of cell content, allow add row/column above/below, allow delete rows/columns.
  [ ] support drag and drop to add rows. similar to a resize interaction except as you drag the bottom border or right borer of a table, it adds new rows and columns. on release, the rows/columns are acutally 